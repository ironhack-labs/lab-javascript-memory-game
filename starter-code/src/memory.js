var MemoryGame = function (cards) {
  this.cards = cards;
  this.pickedCards = cards;
  this.pairsClicked = 0;
  this.pairsGuessed = 0; 
  this.MemoryGame = 1;
};

MemoryGame.prototype.pickedCards = function () {
};

MemoryGame.prototype.pairsClicked = function () {

};

MemoryGame.prototype.pairsGuessed = function () {
};



MemoryGame.prototype.shuffleCards = function (pickedCards) {
this.$cards = $(this.shuffleCards(this.pickedCards));

};

MemoryGame.prototype.checkIfPair = function (firstCard, secondCard) {
if($("firstCard === secondCard"){
  
})
}

MemoryGame.prototype.isFinished = function () {
};